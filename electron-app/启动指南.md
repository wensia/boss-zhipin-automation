# Electron 启动指南

## 🎯 三种启动方式

### 方式 1: 一键启动（最简单）⭐⭐⭐⭐⭐

```bash
cd electron-app
./start-all.sh
```

**特点：**
- ✅ 自动启动后端、前端、Electron
- ✅ 自动检查服务健康状态
- ✅ 自动清理进程
- ✅ 彩色输出，易于查看
- ✅ 关闭后自动停止所有服务

**适合：** 日常使用，最方便

---

### 方式 2: 快速启动（需要手动启动后端和前端）⭐⭐⭐⭐

**前提：** 后端和前端已经在运行

```bash
cd electron-app
./start-dev.sh
```

**特点：**
- ✅ 检查后端和前端服务
- ✅ 自动启动 Electron
- ✅ 适合开发调试

**适合：** 已经有服务在运行时使用

---

### 方式 3: 手动启动（完全控制）⭐⭐⭐

#### 终端 1 - 后端
```bash
cd backend
uv run python -m app.main
```

#### 终端 2 - 前端
```bash
cd frontend
npm run dev
```

#### 终端 3 - Electron
```bash
cd electron-app
npm start
```

**适合：** 需要查看详细日志时使用

---

## 📋 当前状态检查

### 后端服务是否运行？
```bash
curl http://localhost:27421/api/health
```

**成功：** `{"status":"ok","message":"Boss直聘自动化API运行正常"}`

### 前端服务是否运行？
```bash
curl http://localhost:13601
```

**成功：** 返回 HTML 内容

### 查看运行的进程
```bash
ps aux | grep -E "python.*app.main|vite.*13601|Electron"
```

---

## 🛑 如何停止

### 停止 Electron
- **GUI方式：** 点击窗口关闭按钮
- **终端方式：** 在启动 Electron 的终端按 `Ctrl+C`

### 停止所有服务（使用 start-all.sh 时）
```bash
# 在 start-all.sh 运行的终端按 Ctrl+C
# 会自动清理所有进程
```

### 手动停止所有进程
```bash
# 停止后端
pkill -f "uv run python -m app.main"

# 停止前端
pkill -f "vite.*13601"

# 停止 Electron
pkill -f Electron
```

---

## ⚠️ 常见问题

### Q1: 提示端口已被占用
```
ERROR: [Errno 48] Address already in use
```

**解决：**
```bash
# 查找占用端口的进程
lsof -i :27421

# 终止进程
kill -9 <PID>
```

### Q2: Electron 窗口没有打开

**检查清单：**
1. 后端是否运行？`curl http://localhost:27421/api/health`
2. 前端是否运行？`curl http://localhost:13601`
3. 查看终端是否有错误信息

### Q3: 依赖未安装

**症状：**
```
Error: Cannot find module 'electron'
```

**解决：**
```bash
cd electron-app
npm install
```

### Q4: 进程残留

**症状：** 关闭后进程仍在运行

**解决：**
```bash
# 清理所有相关进程
pkill -9 -f "python.*app.main"
pkill -9 -f "vite.*13601"
pkill -9 -f "Electron"
pkill -9 -f "chromium"
pkill -9 -f "playwright"
```

---

## 📊 服务地址

| 服务 | 地址 | 用途 |
|------|------|------|
| 后端 API | http://localhost:27421 | FastAPI 服务 |
| 前端页面 | http://localhost:13601 | React 应用 |
| Electron | 桌面应用窗口 | 集成界面 |

---

## 🎨 启动脚本对比

| 特性 | start-all.sh | start-dev.sh | npm start |
|------|--------------|--------------|-----------|
| 自动启动后端 | ✅ | ❌ | ❌ |
| 自动启动前端 | ✅ | ❌ | ❌ |
| 健康检查 | ✅ | ✅ | ❌ |
| 自动清理 | ✅ | ❌ | ❌ |
| 日志输出 | 彩色 | 彩色 | 标准 |
| 适用场景 | 日常使用 | 开发调试 | 手动控制 |

---

## 💡 推荐工作流

### 第一次使用
```bash
cd electron-app
npm install        # 安装依赖
./start-all.sh     # 一键启动
```

### 日常开发
```bash
# 如果需要频繁修改代码
./start-all.sh     # 一站式启动

# 或者分开启动，方便查看日志
终端1: cd backend && uv run python -m app.main
终端2: cd frontend && npm run dev
终端3: cd electron-app && npm start
```

### 只测试 Electron
```bash
# 确保后端和前端在运行
./start-dev.sh     # 快速启动
```

---

## 📝 日志位置

使用 `start-all.sh` 时，日志保存在：

- **后端日志：** `/tmp/boss-backend.log`
- **前端日志：** `/tmp/boss-frontend.log`

**查看实时日志：**
```bash
# 后端
tail -f /tmp/boss-backend.log

# 前端
tail -f /tmp/boss-frontend.log
```

---

## 🚀 快速命令速查

```bash
# 一键启动所有服务
./start-all.sh

# 只启动 Electron（需要后端和前端已运行）
./start-dev.sh

# 直接启动 Electron
npm start

# 停止所有进程
pkill -f "python.*app.main|vite.*13601|Electron"

# 检查服务状态
curl http://localhost:27421/api/health  # 后端
curl http://localhost:13601              # 前端

# 查看运行的进程
ps aux | grep -E "python|vite|Electron"

# 查看端口占用
lsof -i :27421  # 后端
lsof -i :13601  # 前端
```

---

## 🎯 总结

**最简单的方式：**
```bash
cd electron-app && ./start-all.sh
```

这一个命令就能启动所有服务并打开 Electron 应用！🎉
